<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="mab.moneymanagement.view.fragment.ExpectedFragment">

    <!-- TODO: we will make expected here on list  the row of list on row expected layout  -->
    <TextView

        android:textStyle="bold"
        android:id="@+id/expected_tv_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="15dp"
        android:textColor="@android:color/black"
        android:layout_marginTop="15dp"
        android:visibility="gone"
        android:layout_marginLeft="10dp"


        />

    <ListView
        android:id="@+id/expected_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></ListView>
</LinearLayout>
